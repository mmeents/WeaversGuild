# addClassImport

Adds a new class import model to an existing class. makes private _var and sets via constructor and di.

## Parameters

| Parameter Name | Type | Description |
| :--- | :--- | :--- |
| `classItemId` | number | The ID of the class item. |
| `importClassId` | number | The ID of the class model to import. |