# getTypeDetails

WeItemType enum has two main ranges, 1 > x < OrganizationModel are lookup types.(property options)  OrganizationModel and above are tree node types.

## Description
Lookup details of an item type id.

## Parameters
- **itemTypeId** (number, required): Item Type Id to lookup